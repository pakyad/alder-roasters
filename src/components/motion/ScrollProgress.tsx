/**
 * Reading-progress hairline. Pure CSS (scroll-driven animation in
 * globals.css); renders nothing extra where unsupported.
 */
export function ScrollProgress() {
  return <div className="scroll-progress" role="presentation" />;
}
